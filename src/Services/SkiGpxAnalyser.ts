import {GpxRecord} from '../model/GpxRecord.ts';

export class SkiGpxAnalyser {
    findElevatorsSegments(aGpxRecords: GpxRecord): number {

        aGpxRecords.positionsRecords.forEach((value, index) => {

        })

        return 3;
    }

}
